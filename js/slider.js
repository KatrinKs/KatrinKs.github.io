// Sliders initialization (achievements and books)
class Slider {
    constructor() {
        // Sliders work with CSS only, no JS needed
        // This file is for potential future enhancements
        console.log('Sliders ready');
    }
}

document.addEventListener('DOMContentLoaded', () => new Slider());